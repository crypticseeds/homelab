# Authelia – copy to .env or use from repo root. Never commit .env.
# Run compose with --env-file ../.env so these are available for Docker secrets.

AUTHELIA_JWT_SECRET=generate_long_random_string
AUTHELIA_SESSION_SECRET=generate_long_random_string
AUTHELIA_STORAGE_PASSWORD=your_storage_postgres_password
AUTHELIA_STORAGE_ENCRYPTION_KEY=generate_long_random_string
