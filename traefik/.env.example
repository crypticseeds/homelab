# Traefik – copy to .env or use from repo root. Never commit .env.

CLOUDFLARE_EMAIL=you@example.com
# Create Docker secret: echo -n "YOUR_TOKEN" | docker secret create cf_dns_token -
CF_DNS_API_TOKEN=your_cloudflare_dns_api_token
# Create Docker secret: htpasswd -nb user password | docker secret create basic_auth_credentials -
TRAEFIK_DASHBOARD_CREDENTIALS=user:hashed_password

TZ=Europe/London
